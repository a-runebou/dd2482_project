# Base URL for the API. Leave unset to use the relative /api/v1 path on the app's own origin.
VITE_API_BASE_URL=
# Set to "enabled" to run the in-browser MSW mock worker in development (see npm run dev:mock).
VITE_API_MOCKING=
